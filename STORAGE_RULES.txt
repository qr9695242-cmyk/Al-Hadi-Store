// Firebase Console > Storage > Rules mein jaakar yeh paste kar ke Publish karein:
// (Yeh product photos ko upload/read karne ki ijazat deta hai — admin panel
// ka login real Firebase Auth session nahi banata, is liye Firestore rules
// ki tarah yahan bhi open write rakhni zaroori hai.)

rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /products/{allPaths=**} {
      allow read: if true;
      allow write: if true;
    }
  }
}
